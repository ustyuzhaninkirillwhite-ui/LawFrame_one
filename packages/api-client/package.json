{
  "name": "@lexframe/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "tsc -p tsconfig.json && node dist/settings-client.test.js && node dist/chat-client.test.js"
  },
  "dependencies": {
    "@lexframe/contracts": "workspace:*",
    "@lexframe/workflow-dsl": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
