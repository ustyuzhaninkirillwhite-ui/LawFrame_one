# LexFrame to Activepieces Token Map

Canonical Stage 17.8 mapping is stored in:

- `packages/design-system-activepieces-bridge/src/tokens/lexframe-to-activepieces.map.ts`
- `docs/stage17/17.8/design-token-mapping.md`

LexFrame code should consume stable `--lf-*` aliases and bridge recipes instead
of direct Activepieces internal variable names.
