# OpenSearch Contract

## Stage 0 Position

OpenSearch is documented as downstream search infrastructure, not yet required runtime.

## Rules

- Frontend gets no direct OpenSearch access.
- Search ingestion depends on document classification and source isolation.
- Citation-based drafting remains blocked until index, hybrid retrieval, and source validation exist.

