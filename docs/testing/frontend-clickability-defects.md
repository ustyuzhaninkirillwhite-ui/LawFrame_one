# Frontend Clickability Defects

| Severity | Area | Defect | Evidence | Action |
| --- | --- | --- | --- | --- |
| P1 | AppShell / automation canvas | `/app/projects/:projectId/automations/:automationId/automation` is bounded as an immersive canvas route, but `AppShell` still renders `FloatingAiComposer` on canvas routes. Prompt 2 asks for the floating composer to be hidden on immersive routes; changing it would alter visible behavior, so Block 2 records it instead of changing design. | `apps/web/src/components/app-shell.tsx`, `app-shell.test.tsx` now covers bounded canvas mode; e2e duplicate-composer guard allows only one composer. | Product/design decision needed before hiding the composer on canvas routes. |
| P2 | ProjectSidebar disabled states | Disabled sidebar controls use native `disabled` and opacity, but do not consistently expose a visible disabled reason. | New chat can be disabled when project context is absent; create project submit is disabled when name is blank. | Add visible/ARIA disabled reason in a visual/accessibility pass. |
| P3 | ProjectSidebar search | Search panel opens by keyboard/click, but `Escape` does not close the search panel. | `frontend-keyboard-accessibility.spec.ts` avoids asserting search Escape close because current UX does not implement it. | Add Escape close behavior in a non-visual interaction pass. |
| P3 | Settings focus return | Settings dialog has Escape close and Tab trapping, but focus return to the original trigger is not fully asserted across remounts/collapsed sidebar. | `settings-button.test.tsx` covers persistence/close; e2e covers reachability. | Add focus-return contract once dialog ownership is clarified. |
| ENV | Local e2e environment | Full Block 2 Playwright run could not pass in this workspace because the backend runtime attempted to connect to PostgreSQL on `127.0.0.1:54322` and received `ECONNREFUSED`. This caused `/auth/bootstrap`, project snapshot, project knowledge and chat/search requests to emit controlled/unexpected backend errors. | `corepack pnpm test:block2:e2e` with `LEXFRAME_E2E_USE_MSW=1` produced 7 passed / 16 failed before DB-dependent routes finished. Screenshot evidence was still generated for passed surfaces. | Start the local Supabase/Postgres readiness stack or run against an integrated environment, then rerun `test:block2:e2e`. |
