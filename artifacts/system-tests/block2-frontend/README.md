# Block 2 Frontend Evidence

This directory is reserved for Prompt 2 frontend shell/clickability evidence.

Playwright writes screenshots to:

```text
artifacts/system-tests/block2-frontend/screenshots/
```

The screenshots are evidence captures only. They are not CI baselines and should not be auto-updated as visual snapshots.
