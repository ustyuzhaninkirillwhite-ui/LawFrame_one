# Block 4 Automation / Activepieces Evidence

This directory is reserved for Block 4 system-test evidence.

Expected generated outputs:

- `screenshots/`
- `traces/`
- `session-scan.json`
- `run-evidence.json`
- `network-scan.json`

The committed files are lightweight placeholders. Live Playwright runs can write screenshots/traces here without changing automation or Canvas visuals.
