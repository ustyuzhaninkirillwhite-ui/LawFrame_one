# Block 5 Performance Evidence

Machine-readable performance metrics are written to `metrics/*.json` by `tests/e2e/utils/performance.ts`.

Live E2E runs should append route, click, animation and iframe timing records here.
